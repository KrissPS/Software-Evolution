IDENTIFICATION DIVISION.
PROGRAM-ID. MAIN.

DATA DIVISION.
01 MSG PICTURE IS X(10).

PROCEDURE DIVISION.
    MOVE "HELLO" TO MSG
    DISPLAY "MAIN BEFORE SINGLE"
    CALL CALL-ECHO-ONE-ARG USING MSG
    DISPLAY "MAIN AFTER SINGLE".
